% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/continuous_fragility.R
\name{reverse_continuous_fragility_index_summary}
\alias{reverse_continuous_fragility_index_summary}
\title{Reverse Continuous Fragility Index}
\usage{
reverse_continuous_fragility_index_summary(
  mean1,
  sd1,
  n1,
  mean2,
  sd2,
  n2,
  conf.level = 0.95,
  n_sim = 5L,
  tol_mean = 0.01,
  tol_sd = 0.01,
  max_iter = 10000L,
  seed = NULL
)
}
\arguments{
\item{mean1, sd1, n1}{Mean, standard deviation, and sample size of arm 1.}

\item{mean2, sd2, n2}{Mean, standard deviation, and sample size of arm 2.}

\item{conf.level}{Confidence level for the Welch t-test (default \code{0.95}).}

\item{n_sim}{Number of simulated datasets to average over (default \code{5}).}

\item{tol_mean, tol_sd}{Relative tolerances for rejection sampling.}

\item{max_iter}{Maximum additional participants per arm before giving up
and returning \code{NA_real_} (default \code{10000}).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A single numeric value: mean additional participants per arm
required to reach significance across \code{n_sim} simulations. Returns \code{0}
if the original test was already significant.
}
\description{
Estimates how many additional participants per arm would have been required
to render a non-significant Welch t-test significant, given two-arm summary
statistics. This is a continuous-outcome analogue of the reverse fragility
index for dichotomous outcomes: a measure of how far a non-significant
trial was from significance, expressed in participants per arm.
}
\details{
If the original test is already significant the function returns \code{0}.
Otherwise additional participants are sampled from each arm's assumed
normal distribution (parameterised by the supplied mean and SD) and added
one per arm per iteration until significance is reached. The procedure is
repeated \code{n_sim} times and the mean is returned.
}
\examples{
reverse_continuous_fragility_index_summary(
  mean1 = 55, sd1 = 10, n1 = 30,
  mean2 = 50, sd2 = 10, n2 = 30,
  seed  = 1
)
}
